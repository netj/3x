#!/usr/bin/env bash
set -eu

./customize .build/ 3x.sh <<PARAMS
@BINDIR@=$BINDIR
@TOOLSDIR@=$TOOLSDIR
@LIBDIR@=$LIBDIR
@DATADIR@=$DATADIR
@GUIDIR@=$GUIDIR
@DOCSDIR@=$DOCSDIR
PARAMS
chmod +x .build/3x.sh

cd .build
ln -sf ../package.json .
date >README.md
npm install
